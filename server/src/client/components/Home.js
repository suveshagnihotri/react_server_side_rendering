import React from 'react';

const Home = () => {
	return (
		<div>
			<div>This is Home Component</div>
			<button onClick={() => console.log('on Press Button')}> click me</button>
		</div>
	);
};

export default Home;
